# FastAPI + Server
fastapi==0.115.6
uvicorn[standard]==0.34.0
python-multipart==0.0.18

# Database
sqlalchemy[asyncio]==2.0.36
asyncpg==0.30.0
alembic==1.14.0
psycopg2-binary==2.9.10

# Auth
python-jose[cryptography]==3.3.0
bcrypt==4.0.1
# NOTE: passlib removed in v2.38.6 — it's unmaintained and silently breaks
# password verification with bcrypt >= 4.0. We now use bcrypt directly.

# Task Queue
celery[redis]==5.4.0
redis==5.2.1

# PDF Generation
weasyprint==63.1
jinja2==3.1.4
pypdf==5.1.0
reportlab==4.2.5

# Geospatial (flight path maps + area calc)
shapely==2.0.6
pyproj==3.7.0
staticmap==0.5.7
folium==0.18.0

# LLM — Claude API
anthropic>=0.40.0

# HTTP Client (for OpenDroneLog + Ollama)
httpx==0.28.1

# Email
aiosmtplib==3.0.2

# Rate Limiting
slowapi==0.1.9

# Payments
stripe==11.4.1

# Utilities
pydantic==2.10.3
pydantic-settings==2.7.0
python-dotenv==1.0.1
Pillow==11.0.0

# Structured JSON logging — Phase 5 observability pre-req.
# Replaces the plain `logging.basicConfig(format=...)` setup in app/main.py.
# Alloy discovers containers by com.barnardhq.* labels, so log shape change
# is safe at the collector; any downstream greps over plaintext `[LEVEL]`
# prefixes will need updating.
python-json-logger==3.2.1

# Observability — Sentry/GlitchTip + OpenTelemetry. DSN/endpoint gated in
# app/observability/{sentry,otel}.py — unset env = no-op, so dev and any
# self-hosted single-tenant install keeps working without the central
# plane. See docs/adr/0001-observability.md.
sentry-sdk[fastapi,celery,sqlalchemy]>=2.18.0
opentelemetry-api>=1.28.0
opentelemetry-sdk>=1.28.0
opentelemetry-exporter-otlp>=1.28.0
opentelemetry-instrumentation-fastapi>=0.49b0
opentelemetry-instrumentation-celery>=0.49b0
opentelemetry-instrumentation-sqlalchemy>=0.49b0
opentelemetry-instrumentation-httpx>=0.49b0
opentelemetry-instrumentation-logging>=0.49b0
