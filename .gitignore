# Dependencies
node_modules/
__pycache__/
*.pyc
.venv/
venv/

# Build output
frontend/dist/
*.egg-info/
flight-parser/target/

# Environment / secrets (ADR-0012 — comprehensive)
.env
.env.*
!.env.example
*.env.local
.env.demo.local
.env.bak.*
# private keys / certs
*.pem
*.key
*.p12
*.pfx
*.crt
.secrets/
secrets.yaml
secrets.yml
# misc local secret stores
.netrc
.pgpass
google-credentials.json
service-account*.json

# IDE
.vscode/
.idea/

# Claude Code local settings
.claude/settings.local.json

# Data volumes
/data/

# OS
.DS_Store
Thumbs.db

# Python
*.pyo
*.pyd
.Python
pip-log.txt

# Docker
docker-compose.override.yml
.last_deployed_commit
.autopull.lock
autopull.log
.claude/worktrees/
# (.env.demo already covered by `.env.*` above — kept here as a comment so
# operators searching for it know the rule lives in the secrets section.)
