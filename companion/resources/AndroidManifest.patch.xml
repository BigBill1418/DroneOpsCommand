<!--
  These permissions and attributes must be present in the final AndroidManifest.xml
  after running `npx cap add android`. The BUILD.md instructions explain where to
  add them. This file is a reference, not applied automatically.

  File: android/app/src/main/AndroidManifest.xml
-->

<!-- Add these permissions BEFORE the <application> tag: -->
<uses-permission android:name="android.permission.INTERNET" />
<uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
<uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />

<!-- Add this attribute to the <application> tag: -->
<!-- android:requestLegacyExternalStorage="true" -->

<!--
  Full example <application> tag:

  <application
      android:allowBackup="true"
      android:icon="@mipmap/ic_launcher"
      android:label="@string/app_name"
      android:requestLegacyExternalStorage="true"
      android:theme="@style/AppTheme"
      ...>
-->
